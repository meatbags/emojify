import { connect } from 'react-redux';
import CanvasOutput from '../components/canvas/canvas_output';

const mapDispatchToProps = dispatch => ({});
const mapStateToProps = state => ({});

export default connect(mapStateToProps, mapDispatchToProps)(CanvasOutput);
