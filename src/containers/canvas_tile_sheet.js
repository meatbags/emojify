import { connect } from 'react-redux';
import CanvasTileSheet from '../components/canvas/canvas_tile_sheet';

const mapDispatchToProps = dispatch => ({});
const mapStateToProps = state => ({});

export default connect(mapStateToProps, mapDispatchToProps)(CanvasTileSheet);
